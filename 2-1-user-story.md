# 2-1. User Story — sTTak

본 문서는 sTTak의 **타겟층 / 사용 사례(User Stories) / 확장 시나리오**를 정리한다.
백엔드(`sttak-backend-demo`)는 이 스토리를 만족시키는 계약을 제공해야 한다.

문서 표기:

- **U+숫자** — User Story 식별자 (`U1`, `U2`, …)
- 사용자 스토리는 “**As a / I want / So that**” 형식을 따른다.

---

## 2-1.1 타겟층 (Target Personas)

### Primary — “감 매수에서 벗어나고 싶은 20\~30대 초보 투자자”

| 항목 | 내용 |
| --- | --- |
| 연령 | 20\~30대 |
| 투자 경력 | 3년 미만 |
| 보유 자산 | 종잣돈 부족, 본업 중심 |
| 핵심 고통 | (1) 정보 과부하 (2) 용어 지옥 (3) 인과관계 판단의 한계 |
| 행동 특성 | 3줄 넘는 텍스트는 읽지 않음 · 매수 후 시세 외 정보를 잘 보지 않음 · FOMO에 취약 |
| 목표 | "감"이 아닌 "팩트"로 첫 매수를 결정하고 싶다 |

### Secondary — “용어부터 다시 배우려는 재진입 투자자”

| 항목 | 내용 |
| --- | --- |
| 특징 | 한 번 손실을 경험하고 시장에서 잠시 이탈한 사용자 |
| 니즈 | 실거래 전에 모의로 감각을 회복하고 싶다 · 차트 지표를 한 번에 정리하고 싶다 |
| 행동 | 차트 학습 + 모의투자 사용 빈도가 가장 높음 |

### Tertiary — “금융 교육 컨텐츠로서의 활용자”

| 항목 | 내용 |
| --- | --- |
| 예시 | 대학교 투자 동아리, 금융 학회, 교양 강의 |
| 니즈 | 안전한(실거래 X) 환경에서 학습 사이클(공부 → 적용 → 피드백)을 체험시키고 싶다 |
| 사용 패턴 | 퀴즈 / 랭킹 위주, 그룹 단위 경쟁 |

### Non-targets (명시적 비대상)

- 단타·스캘핑 등 초단위 실시간 시세가 필요한 사용자
- 전문 트레이더용 고급 지표·자동매매 사용자

---

## 2-1.2 사용 사례 (User Stories)

### A. 온보딩 / 인증

| ID | User Story | 수용 기준 (Acceptance) |
| --- | --- | --- |
| **U1** | 초보 투자자로서, **카카오 / 구글 / 애플** 중 하나로 빠르게 로그인하고 싶다. 그래야 별도의 회원가입 부담 없이 서비스를 바로 써볼 수 있다. | 소셜 토큰이 서버에서 검증되고 자체 JWT(access + refresh)가 발급된다. |
| **U2** | 초보 투자자로서, 가입 직후 **관심 종목을 최대 5개까지** 검색·선택하고 싶다. 그래야 이후 피드와 퀴즈가 내 관심사 중심으로 채워진다. | 종목 검색이 가능하고, 6개째 추가 시 거부된다(최대 5개 불변식). 선택한 종목은 사용자 계정에 영속된다. |
| **U3** | 사용자로서, 기존 로그인 정보가 만료되면 **자동으로 토큰이 갱신**되어 끊김 없이 서비스를 계속 쓰고 싶다. | refresh 토큰으로 access 토큰을 재발급할 수 있다. refresh 만료 시 재로그인이 요구된다. |

### B. 홈 — “3초 브리핑”

| ID | User Story | 수용 기준 |
| --- | --- | --- |
| **U4** | 초보 투자자로서, 앱을 열자마자 오늘 내 관심 종목의 **무드(긍정/중립/부정)** 를 한눈에 보고 싶다. 그래야 어디부터 봐야 할지 결정할 수 있다. | 관심 종목별 시세 + 뉴스 분류 결과를 합쳐 무드와 종목 리스트를 단일 응답으로 반환한다. |
| **U5** | 초보 투자자로서, 가장 중요한 종목 카드를 **빠르게 훑고** 싶다. | 종목 카드 응답에 100자 요약과 무드 라벨이 포함된다. |
| **U6** | 초보 투자자로서, 뉴스 카드 하나를 선택하면 **(1) 쉬운 말 풀이 → (2) 원문 → (3) AI 챗봇** 순으로 깊이를 조절해 가며 보고 싶다. | 뉴스 상세 응답이 easy / original / source 세 필드를 제공한다. AI 응답은 별도 챗봇 경로가 스트리밍으로 처리한다. |

### C. 차트 학습 + 모의투자

| ID | User Story | 수용 기준 |
| --- | --- | --- |
| **U7** | 초보 투자자로서, **종목별 캔들과 거래량 시계열**을 기간/구간 단위로 조회하고 싶다. 그래야 지표 학습과 모의 매매에 사용할 수 있다. | 종목 코드와 기간(from/to)으로 캔들 시계열을 조회할 수 있다. |
| **U8** | 초보 투자자로서, **가상 자본금 1,000만 원**으로 안전하게 매수·매도를 연습하고 싶다. | 신규 계정에 초기 자본금 1,000만 KRW가 부여된다. 모의 매매는 실제 거래소 체결과 무관하다. |
| **U9** | 초보 투자자로서, 매수 직전에 **“왜 사는가”에 대한 근거를 한 줄로 입력**하도록 강제받고 싶다. 그래야 감으로 매수하는 습관이 줄어든다. | 매매 요청에 매수 근거(rationale)가 비어 있으면 거절된다(`VALIDATION`). 입력값은 거래 기록에 영속된다. |
| **U10** | 초보 투자자로서, 매매 직후 **AI가 내 매수 근거와 실제 결과를 비교한 회고**를 받아 보고 싶다. | 매매 종료 시점에 회고가 생성되고, 사용자별 회고 아카이브에 누적된다. |

### D. 퀴즈 + 가상 자본금 보상

| ID | User Story | 수용 기준 |
| --- | --- | --- |
| **U11** | 초보 투자자로서, **6시간마다 3문항**을 풀고 싶다. 한 문항을 풀면 곧바로 정답 여부와 해설을 보고 다음 문항으로 넘어가고, 다 풀고 나서는 "결과 보기"로 3문항을 한눈에 정리해서 보고 싶다. | 한 번에 한 문항씩 응답하고 즉시 채점·해설을 받는다. 한 사이클 안에서 최대 3문항을 풀 수 있고, 3번째 응답 직후 6시간 쿨다운이 적용된다. 결과 화면에서는 3문항 각각의 (문항·내가 고른 선지·정답 선지·정답 여부·해설) 과 합계가 노출된다. 문항은 **AI가 랜덤 생성**한다. |
| **U12** | 초보 투자자로서, 퀴즈를 맞히면 **가상 자본금으로 보상**을 받고 싶다. 그래야 학습이 모의투자로 자연스럽게 이어진다. | 정답 수 기반으로 가상 자본금이 충전된다. 별도의 “포인트” 통화는 없고, 보상 통화는 가상 자본금 한 가지로 통일된다. |

### E. 랭킹

| ID | User Story | 수용 기준 |
| --- | --- | --- |
| **U13** | 초보 투자자로서, **총 자산(현금 + 보유 종목 평가액) 기준 리더보드**에서 내 순위를 보고 싶다. 그래야 모의투자에 게임처럼 동기가 붙는다. | 단일 리더보드가 총 자산 기준으로 정렬되어 제공된다. 본인 순위는 별도로 함께 제공된다. |
| **U14** | 초보 투자자로서, MVP 단계에서는 **다른 사용자가 적더라도** 자연스럽게 보이는 리더보드를 보고 싶다. | MVP 기간 동안 타 사용자는 모킹 데이터로 채워질 수 있다. 본인 순위·자산은 실제 데이터를 사용한다. |

### F. 마이페이지

| ID | User Story | 수용 기준 |
| --- | --- | --- |
| **U15** | 사용자로서, 내 **현재 포트폴리오(현금 잔고, 보유 종목, 평가 손익)** 를 단일 응답으로 조회하고 싶다. | 포트폴리오 응답에 현금/보유/총평가/손익률이 포함된다. |
| **U16** | 사용자로서, 과거 **모든 모의 매매 기록**을 시간순으로 조회하고 싶다. | 거래 내역 조회는 페이징을 지원하며, 각 항목은 종목 / 매매 종류 / 수량 / 체결가 / 매수 근거를 포함한다. |
| **U17** | 사용자로서, AI가 생성한 **회고 카드들을 아카이브 형태로 다시 꺼내** 보고 싶다. 그래야 같은 실수를 반복하지 않는다. | 회고 아카이브는 즉시 회고를 시간순으로 반환한다. |

### G. 챗봇 (Chat)

| ID | User Story | 수용 기준 |
| --- | --- | --- |
| **U18** | 초보 투자자로서, 차트·뉴스·자유 질문에 대해 **AI 챗봇과 대화**하고 싶다. 그래야 모르는 용어와 상황 해석을 즉시 해결할 수 있다. | 챗봇은 세 가지 컨텍스트(뉴스 / 차트 구간 / 자유 질문)를 지원하고, 응답은 스트리밍으로 전송된다. |
| **U19** | 초보 투자자로서, 챗봇 응답에 **"무조건 올라간다", "사라"** 같은 추천 표현이 없기를 바란다. 그래야 AI 응답을 학습 보조로 신뢰할 수 있다. | LLM 가드레일이 추천성 표현을 차단한다. 정보 출처 링크가 함께 포함된다. |

---

## 2-1.3 핵심 사용 흐름 (Primary Flows)

사용자 입장에서 자주 반복되는 시나리오 흐름을 요약한다. 실제 엔드포인트는 `3-3-contract.md`에서 확정한다.

### Flow 1. 신규 사용자 첫 진입

```
소셜 로그인 → 토큰 검증/JWT 발급 → 관심 종목 5개 등록
   → 홈 3초 브리핑 (관심 종목 시세 + 분류된 뉴스 요약)
   → 뉴스 카드 선택 → 쉬운 말 풀이 → 원문 → 챗봇
```

### Flow 2. 차트 학습 → 모의 매수 → 회고

```
캔들·시세 조회 → 매수 요청 ("왜 사는가" 필수) → 가상 체결
   → 즉시 회고 생성 → 회고 아카이브 누적
```

### Flow 3. 퀴즈 → 가상 자본금 충전 → 랭킹 반영

```
다음 문항 조회 (쿨다운 확인) → 1 문항 응답 → 즉시 채점 + 해설 + 자본금 적립
   → (반복, 최대 3회) → 결과 보기 (3문항 상세)
   → 총 자산 변동 → 랭킹 재계산
```

---

## 2-1.4 확장 시나리오 (Extension Scenarios)

MVP 이후를 고려해, 백엔드가 **계약 설계 시점부터 일반화해 두어야 하는** 차원들이다.

| 차원 | MVP 결정 | 확장 시 무엇이 달라지나 | 백엔드가 지금 해 둘 일 |
| --- | --- | --- | --- |
| **국제화 (Internationalization)** | KRW · 국내 종목 한정 | USD 등 통화, 해외 거래소(NYSE/NASDAQ) 추가 | `Stock`에 `market/exchange`, `Money`에 `currency` 필드를 **지금부터** 보유한다 |
| **타 사용자 데이터** | 랭킹 타 사용자는 모킹 가능 | 실제 사용자 기반 리더보드 | 랭킹 스키마는 실사용자 기반으로 설계 (모킹은 응답 레이어에서만) |
| **거래 유형** | 매수 / 매도 단순 체결 | 분할 매수, 평균단가 추적, 익절·손절 알림 | `Trade`에 type 필드, Portfolio에 평균단가/실현손익 분리 |
| **퀴즈 형식** | 4지선다 3문항 / 6h | OX, 빈칸 채우기, 시뮬레이션 퀴즈 | `Quiz` 도메인이 `QuestionKind` 추상화를 갖도록 |
| **랭킹 종류** | 총 자산 단일 리더보드 | 친구 그룹·동아리 그룹·기간별 리더보드 | 랭킹 Port에 `scope`(global/group), `period`(daily/weekly) 파라미터 |
| **회고 트리거** | 매매 직후 + 1개월 후 | 종목별 손익 임계치 도달 시, 사용자 요청 시 | Retrospective 생성을 Port로 추상화해 트리거 타입 다양화 |
| **차트 데이터 소스** | data.go.kr 금융위 오픈API(일봉, [ADR-010](../decisions/ADR-010-market-data-source-datagokr.md)) | 분봉·실시간·해외 데이터 벤더 추가 | 시세 어댑터를 `sttak-external`에서 벤더별 구현체로 분리 |
| **LLM 모델** | Claude Haiku(분류·퀴즈) / Sonnet(챗봇·회고) | 모델 교체, 비용 최적화 라우팅 | LLM Port를 단일화 — 모델 선택은 어댑터/설정에서 결정 |
| **B2B2C 임베드** | 직접 운영 | 증권사·교육사에 SDK/API 임베드 | API 응답에 추천성 표현이 들어가지 않음을 **계약 수준에서 보장** |

### 푸시 알림 (Push Notification)

MVP에서는 푸시 알림을 도입하지 않는다. 다음 시점에 도입을 검토한다.

| ID | 시나리오 | 트리거 | 백엔드가 지금 해 둘 일 |
| --- | --- | --- | --- |
| **EX-N1** | 관심 종목 중요 공시·뉴스 알림 | 데이터 파이프라인이 “중요” 등급으로 분류한 이벤트 | 뉴스 분류 결과에 `importance` 필드를 보유, 사용자×종목 구독 관계 모델링 |
| **EX-N2** | 퀴즈 쿨다운 종료 알림 | 6시간 쿨다운 만료 | 사용자별 다음 쿨다운 종료 시각을 영속화해 두기 |

확장 시 공통:

- Notification 도메인은 **채널 무관(channel-agnostic) Port**로 설계한다. 첫 채널은 푸시지만,
  이메일·카카오 알림톡 등으로 확장 가능해야 한다.
- 알림 발송 자체는 사용자 응답 경로를 막지 않도록 **비동기 이벤트**로 처리한다.
- 사용자별 알림 수신 동의(opt-in/out)는 도입 시점에 함께 정의한다.

---

## 2-1.5 후속 문서

| 문서 | 연결 |
| --- | --- |
| `2-2-requirements.md` | 본 문서의 User Story → 기능/비기능 요구사항으로 분해 |
| `3-3-contract.md` | 위 핵심 흐름의 API를 OpenAPI 계약으로 명세 |
| `3-1-server-architecture.md` | 두 갈래 처리 구조(실시간 vs 배치) 상세 |
