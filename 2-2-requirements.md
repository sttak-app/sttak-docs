# 2-2. Requirements — sTTak 백엔드

본 문서는 `2-1-user-story.md`의 User Story(`U1`\~`U19`)와 확장 시나리오(`EX-*`)를
**개발자가 구현·검수 가능한 단위**의 기능/비기능 요구사항과 제약으로 분해한다.

표기:

- **FR-xxx** — Functional Requirement (기능 요구)
- **NFR-xxx** — Non-Functional Requirement (비기능 요구)
- **CON-xxx** — Constraint (제약)
- 각 요구 끝의 *(← Ux)* 는 해당 User Story 추적 표시.

---

## 2-2.1 기능 요구사항 (Functional Requirements)

### A. 인증 / 사용자

| ID | 요구 |
| --- | --- |
| **FR-A1** | 카카오 / 구글 / 애플 소셜 토큰을 검증하고, 검증 성공 시 자체 JWT(access + refresh) 쌍을 발급한다. *(← U1)* |
| **FR-A2** | refresh 토큰으로 access 토큰을 재발급하는 기능을 제공한다. refresh 만료/위조 시 401과 재로그인 신호를 반환한다. *(← U3)* |
| **FR-A3** | 사용자는 관심 종목을 **최대 5개**까지 등록·해제할 수 있다. 6개째 등록 요청은 `CONFLICT`로 거부한다. *(← U2)* |
| **FR-A4** | 신규 사용자에게 가상 자본금 **10,000,000 KRW**를 초기 부여한다. *(← U8)* |

### B. 홈 / 뉴스

| ID | 요구 |
| --- | --- |
| **FR-B1** | 관심 종목 묶음에 대해 **무드(긍정/중립/부정) + 종목별 카드 리스트**를 단일 응답으로 제공한다. *(← U4, U5)* |
| **FR-B2** | 각 종목 카드 응답에는 **100자 이내 요약 텍스트**와 **무드 라벨**이 포함된다. *(← U5)* |
| **FR-B3** | 뉴스 상세 조회는 **easy(쉬운 말 풀이) / original(원문 텍스트) / source(원문 URL)** 세 필드를 모두 포함한다. *(← U6)* |
| **FR-B4** | 동일 이슈에 대한 중복 기사는 임베딩 클러스터링으로 묶고, 클러스터당 대표 요약 1건만 사용자에게 노출한다. |

### C. 차트 / 모의투자

| ID | 요구 |
| --- | --- |
| **FR-C1** | 종목 코드와 기간(from/to)으로 캔들 시계열(OHLCV)을 조회할 수 있다. **MVP는 일봉(`1D`)만 제공**한다(데이터가 일 단위 — [ADR-010](../decisions/ADR-010-market-data-source-datagokr.md)). *(← U7)* |
| **FR-C2** | 사용자는 매수/매도를 요청할 수 있다. 매수 요청에는 **rationale(매수 근거) 필수**, 비어 있으면 `VALIDATION`으로 거부한다. *(← U9)* |
| **FR-C3** | 모의 매매 체결 시 현금 잔고·보유 종목·평균단가가 트랜잭션 안에서 일관성 있게 갱신된다. *(← U8)* |
| **FR-C4** | 매매 종료 직후, 사용자 매수 근거 + 거래 결과를 입력으로 **즉시 회고**를 생성한다. *(← U10)* |
| **FR-C5** | 모의 매매 체결은 **일봉 기준**이다 — 매수는 **주문일(D) 시가**, 매도는 **주문일(D) 종가**로 체결하며, D의 일봉이 도착하는 **다음 영업일 배치에서 정산**된다(주문 접수 시 `PENDING` → 체결 `FILLED`/`REJECTED`). *(← [ADR-011](../decisions/ADR-011-mock-trade-execution-model.md))* |

### D. 퀴즈

| ID | 요구 |
| --- | --- |
| **FR-D1** | 사용자는 **rolling 6시간 윈도우당 최대 3문항**까지 응답할 수 있다. 윈도우 내 응답 수가 3에 도달하면 쿨다운이 활성화되며, `nextAvailableAt = 최근 3개 응답 중 가장 오래된 응답의 answeredAt + 6h` 시점에 다시 응답할 수 있다. *(← U11)* |
| **FR-D2** | 클라이언트는 한 문항씩 받아 풀고, **응답 즉시 정답 여부·정답 선지·해설·해당 문항의 적립 자본금**을 받는다. 응답 후 다음 문항으로 진행하며, 같은 사이클에서 최대 3문항까지 가능하다. *(← U11)* |
| **FR-D3** | 정답 1문항당 **즉시** 가상 자본금이 충전된다(트랜잭션은 문항 단위). 충전은 `Portfolio` 도메인의 현금 잔고를 증가시키며, 별도의 “포인트” 잔액은 두지 않는다. *(← U12)* |
| **FR-D4** | 퀴즈 문항은 **AI가 랜덤 생성**한다(특정 사용자 Watchlist 의존 X). 백엔드 배치(`sttak-batch`)에서 6시간 주기로 후보 풀을 생성·검수해 두고, 사용자별 다음 문항은 풀에서 미응시 항목을 랜덤 샘플링한다. *(← U11)* |
| **FR-D5** | “결과 보기” 응답은 최근 3개(이하) 응답 각각의 **(문항 본문, 4 선지, 사용자 선택, 정답 선지, 정답 여부, 해설)** 과 합계(정답 수·적립 자본금·마지막 응답 시각)를 반환한다. *(← U11)* |

### E. 랭킹

| ID | 요구 |
| --- | --- |
| **FR-E1** | 총 자산(현금 + 보유 종목 평가액) 기준 단일 리더보드를 제공한다. *(← U13)* |
| **FR-E2** | 리더보드 응답에는 **본인 순위/총 자산**이 별도로 함께 포함된다. *(← U13)* |
| **FR-E3** | MVP 동안 타 사용자 데이터는 모킹된 데이터로 채울 수 있다. 본인 데이터는 항상 실제값을 사용한다. *(← U14)* |

### F. 마이페이지

| ID | 요구 |
| --- | --- |
| **FR-F1** | 포트폴리오 조회는 **현금 잔고 / 보유 종목별 (수량, 평균단가, 평가액) / 총평가액 / 손익률**을 단일 응답으로 제공한다. *(← U15)* |
| **FR-F2** | 거래 내역 조회는 페이징을 지원하고, 각 항목은 *(종목, 매매 종류, 수량, 체결가, rationale, 체결 시각)* 을 포함한다. *(← U16)* |
| **FR-F3** | 회고 아카이브 조회는 즉시 회고를 시간순으로 반환한다(거래 ID로 묶음). *(← U17)* |

### G. 챗봇

| ID | 요구 |
| --- | --- |
| **FR-G1** | 챗봇은 세 가지 컨텍스트 모드 — **뉴스 / 차트 구간 / 자유 질문**을 지원한다. *(← U18)* |
| **FR-G2** | 챗봇 응답은 SSE(Server-Sent Events)로 토큰 단위 스트리밍한다. *(← U18)* |
| **FR-G3** | 모든 챗봇·회고·요약 응답에 **LLM 가드레일**을 적용해 “사라”, “무조건 오른다” 등 추천성 표현을 차단한다. *(← U19)* |
| **FR-G4** | 챗봇 응답에는 사용한 소스(뉴스/공시 URL)를 함께 반환한다. *(← U19)* |

### H. 데이터 파이프라인

| ID | 요구 |
| --- | --- |
| **FR-H1** | 뉴스/공시 수집은 주기적으로 수행한다. 국내 뉴스 1차 수집원은 **Naver 검색 API**(`ADR-002`)이며, NewsAPI/DART OpenAPI는 동일한 `NewsCollectionPort`의 추가 구현으로 공존 가능하다. MVP는 사전 정의한 10종목을 설정으로 고정해 수집한다. 수집 주기와 윈도우는 운영 설정으로 조정 가능해야 한다. |
| **FR-H2** | 수집된 원문에 대해 (1) 종목 태깅 → (2) 클러스터링 → (3) 100자 요약 → (4) 임베딩 적재 단계를 거친다. 단계별로 실패 시 재시도/스킵 정책이 적용된다. |
| **FR-H3** | 시세·종목 데이터는 **data.go.kr 금융위 오픈API 2종**(주식시세정보 `15094808`, KRX상장종목정보 `15094775`)에서 `sttak-batch`가 **일 1회 일봉**으로 수집한다. 인증 `serviceKey`는 **백엔드에만** 존재한다. *(← CON-S1, [ADR-010](../decisions/ADR-010-market-data-source-datagokr.md))* |
| **FR-H4** | 인과 그래프는 뉴스/이슈/종목/가격 변동을 노드/엣지로 표현해 Graph 저장소에 적재한다. (Phase 2) |
| **FR-H5** | 퀴즈 후보 풀은 6시간 배치로 AI가 랜덤 생성한 뒤 검수 단계를 거쳐 저장된다. *(← FR-D4)* |

---

## 2-2.2 비기능 요구사항 (Non-Functional Requirements)

### 성능 / 지연

| ID | 요구 |
| --- | --- |
| **NFR-P1** | 홈 3초 브리핑(`GET /home/briefing`)의 **p95 응답시간 ≤ 1500ms**, p99 ≤ 3000ms. *(← U4)* |
| **NFR-P2** | 일반 조회 API(`GET *`)의 **p95 ≤ 500ms**. |
| **NFR-P3** | 모의 매매(`POST /trades`)의 **p95 ≤ 800ms**. |
| **NFR-P4** | 챗봇 응답은 **TTFB(첫 토큰) ≤ 2초**. 전체 응답은 SSE 스트리밍으로 처리해 사용자 인지 지연을 줄인다. *(← U18)* |
| **NFR-P5** | 인증 API(`POST /auth/*`) p95 ≤ 800ms. |

### 가용성 / 신뢰성

| ID | 요구 |
| --- | --- |
| **NFR-R1** | 외부 의존성(NewsAPI/DART/LLM/시세) 장애가 **사용자 응답 경로를 중단시키지 않는다**. 캐시 폴백 + 서킷브레이커로 graceful degradation을 적용한다. |
| **NFR-R2** | LLM 호출은 클러스터 단위 결과 캐싱을 통해 동일 요청에 대한 중복 호출을 제거한다. |
| **NFR-R3** | 모든 쓰기 API는 멱등성(idempotency)을 고려한다 — 멱등 키가 필요한 엔드포인트는 `3-3-contract.md`에 명시한다. |
| **NFR-R4** | 시스템 월간 가용성 목표 **99.5%** (MVP). |

### 보안 / 프라이버시

| ID | 요구 |
| --- | --- |
| **NFR-S1** | LLM/시세/뉴스 API 키는 **백엔드 환경변수 또는 AWS Secrets Manager에만** 존재한다. 코드/로그/응답 어디에도 노출 금지. |
| **NFR-S2** | 자체 JWT는 **HS256 또는 RS256**으로 서명한다. access 토큰 만료 1시간, refresh 14일을 기본값으로 한다. |
| **NFR-S3** | 비밀번호 입력은 받지 않는다(소셜 로그인 only). 별도 비밀번호 저장 책임 없음. |
| **NFR-S4** | 모든 통신은 **HTTPS**. 운영 환경에서 HTTP는 차단한다. |
| **NFR-S5** | PII(이메일, 이름)는 로그·메트릭에 평문으로 기록하지 않는다. 필요 시 사용자 ID만 사용한다. |

### 일관성 / 데이터

| ID | 요구 |
| --- | --- |
| **NFR-D1** | 포트폴리오·거래·퀴즈는 **서버가 SSOT(단일 진실 공급원)** 이다. 클라이언트는 캐시 역할만 한다. |
| **NFR-D2** | 모의 매매·잔액 변경은 **트랜잭션 안에서 일관성**을 보장한다 (현금 차감 + 보유 종목 증가가 동일 트랜잭션). |
| **NFR-D3** | 외부 데이터(뉴스/시세)는 **지연 데이터(delayed)** 임을 응답에 명시한다 (`fetchedAt` 필드). |

### 관측성

| ID | 요구 |
| --- | --- |
| **NFR-O1** | 모든 HTTP 요청에 **trace ID**가 부여되고, 응답 헤더로 반환된다. *(→ `6-observability.md`)* |
| **NFR-O2** | API별 호출 수, p50/p95/p99 지연, 에러율 메트릭을 노출한다. |
| **NFR-O3** | LLM 호출은 **토큰 사용량 / 모델 / 비용 추정치**를 메트릭으로 기록한다. |

### 확장성 / 운영

| ID | 요구 |
| --- | --- |
| **NFR-X1** | 사용자 응답 경로(`sttak-api`)와 배치 경로(`sttak-batch`)는 **수평 확장 가능**하도록 stateless로 설계한다 (세션 미사용 = JWT). |
| **NFR-X2** | 데이터 파이프라인은 **사용자 트래픽과 분리된 워커**에서 실행한다. *(← 1-background.md §1.4.1)* |

---

## 2-2.3 제약 사항 (Constraints)

### 기술 스택 (CON-T)

| ID | 제약 |
| --- | --- |
| **CON-T1** | 언어 **Java 21** 고정. Kotlin/Groovy 코드는 추가하지 않는다 (테스트 포함). |
| **CON-T2** | 프레임워크 **Spring Boot 3.5.15**, 빌드 **Gradle (Groovy DSL)** 멀티모듈. |
| **CON-T3** | 운영 DB **PostgreSQL 17**. 로컬/테스트는 H2 또는 Testcontainers Postgres 17. |
| **CON-T4** | ORM은 **Spring Data JPA + Hibernate** 한정. MyBatis/jOOQ 도입 금지. |
| **CON-T5** | 인증은 **자체 JWT(access + refresh)**. 세션·쿠키 인증 도입 금지 (stateless 유지). |

### 도메인/스코프 (CON-S)

| ID | 제약 |
| --- | --- |
| **CON-S1** | 외부 API 키(NewsAPI/DART/LLM/시세)는 **백엔드에만** 존재한다. 클라이언트에 절대 노출되지 않는다. *(← NFR-S1)* |
| **CON-S2** | 시세는 **지연 데이터(일봉, 1영업일 지연)** 를 사용한다. 분봉·초단위 실시간은 본 서비스의 범위 밖. *(← [ADR-010](../decisions/ADR-010-market-data-source-datagokr.md))* |
| **CON-S3** | MVP는 **국내 종목(KRX), 통화 KRW** 단일이지만, 도메인 모델은 처음부터 `market/exchange`, `currency` 필드를 보유해 국제화 확장을 막지 않는다. *(← 2-1.4)* |
| **CON-S4** | 본 서비스는 **실거래를 수행하지 않는다**. 모든 매매는 가상 자본금 기반의 모의 매매다. |
| **CON-S5** | AI 응답에서 **투자 추천성 표현은 백엔드 가드레일에서 차단**한다. 책임 한계 및 학습 보조 목적임을 응답 메타에 명시한다. *(인공지능기본법 대응)* |

### 아키텍처 / 코드 (CON-A)

| ID | 제약 |
| --- | --- |
| **CON-A1** | DDD + Hexagonal + CQRS 패턴을 따른다. 도메인 모델은 JPA 어노테이션을 갖지 않는다 (`*JpaEntity`로 분리). *(← ARCHITECTURE.md)* |
| **CON-A2** | 영속 어댑터(`*.persistence`)는 **package-private**. 모듈 외부에서 JPA 엔티티/리포지토리에 직접 접근 금지. |
| **CON-A3** | 트랜잭션 경계는 **Application Service만 소유**한다. Controller·Adapter는 트랜잭션을 시작하지 않는다. |
| **CON-A4** | 도메인 객체는 **public 생성자 금지**. 정적 팩토리 `create`(신규)/`restore`(복원)만 노출. |
| **CON-A5** | 외부 시스템 호출(HTTP/SDK)은 `sttak-external` 모듈에서 **도메인 Port의 구현체**로 작성한다. |
| **CON-A6** | Jacoco 라인 커버리지 **최소 50%** (현재 게이트). 미달 시 `./gradlew build` 실패. |

### 운영 (CON-O)

| ID | 제약 |
| --- | --- |
| **CON-O1** | 환경 프로파일은 `local`, `dev`, `prod` 셋만 사용한다. 그 외는 추가 시 명시 승인. |
| **CON-O2** | DB 마이그레이션은 단일 도구 하나로 관리한다 (Flyway 또는 Liquibase 중 택1 → `7-deployment.md`). |
| **CON-O3** | 운영 배포는 **GitHub Actions** 단일 파이프라인을 통해서만 이루어진다. |

---

## 2-2.4 범위 외 (Out of Scope, MVP 기준)

다음은 명시적으로 **MVP에서 다루지 않는다.**

- 실거래·실제 결제·환전
- 안드로이드 클라이언트 / 반응형 웹
- 비밀번호 기반 회원가입·재설정
- 약관 동의 UI 및 약관 이력 관리 (법무 검토 후 별도 스펙)
- 2FA / OTP / 권한 기억(Remember-Me)
- 푸시 알림 (확장 시나리오 `EX-N1~N2`로 이동)
- 친구/그룹 랭킹, 기간별(daily/weekly) 랭킹
- 자동매매·익절·손절 알림
- 다국어 UI
- 1개월 후속 회고 (사용자 스토리에서 제외)
- Watchlist 기반 맞춤 퀴즈 (퀴즈는 AI 랜덤 생성)

---

## 2-2.5 수용 기준 매트릭스 (요약)

각 User Story가 어떤 FR/NFR로 분해되었는지의 빠른 매핑.

| User Story | 분해 |
| --- | --- |
| U1 | FR-A1, NFR-S2, NFR-P5 |
| U2 | FR-A3 |
| U3 | FR-A2 |
| U4 | FR-B1, NFR-P1 |
| U5 | FR-B1, FR-B2 |
| U6 | FR-B3, FR-G2 |
| U7 | FR-C1 |
| U8 | FR-A4, FR-C3 |
| U9 | FR-C2, NFR-D2 |
| U10 | FR-C4 |
| U11 | FR-D1, FR-D2, FR-D4, FR-D5, FR-H5 |
| U12 | FR-D2, FR-D3 |
| U13 | FR-E1, FR-E2 |
| U14 | FR-E3 |
| U15 | FR-F1 |
| U16 | FR-F2 |
| U17 | FR-F3 |
| U18 | FR-G1, FR-G2, NFR-P4 |
| U19 | FR-G3, FR-G4 |
| EX-N1~N2 | (확장 시 정의, MVP 미포함) |

---

## 2-2.6 후속 문서

| 문서 | 연결 |
| --- | --- |
| `3-1-server-architecture.md` | 본 요구를 어떤 구조로 실현할지 |
| `3-3-contract.md` | FR을 실제 API 시그니처/스키마로 명세 |
| `5-1-test-case.md` | 본 요구의 수용 기준을 어떻게 검증할지 |
| `5-2-error-handling.md` | 요구상 거부/오류 상황(예: `CONFLICT`, `VALIDATION`)의 HTTP 매핑 |
| `6-observability.md` | NFR-O 계열 실현 방법 |
| `7-deployment.md` | CON-O / NFR-X 계열 실현 방법 |
